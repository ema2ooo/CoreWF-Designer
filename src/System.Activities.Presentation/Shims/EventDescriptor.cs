namespace System.Diagnostics.Eventing
{
    internal struct EventDescriptor
    {
        public EventDescriptor(int eventId, byte version, byte channel, byte level, byte opcode, int task, long keyword)
        {
        }
    }
}
